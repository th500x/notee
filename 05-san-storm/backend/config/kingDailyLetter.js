/**
 * AI 君主 · 每日传书（闲聊 + 局势 + 日俸）
 */

/** temp_event_ranking.event_id（与大司空日榜隔离） */
const EVENT_ID = 'san_1_king_daily_letter';

const MAIL_EXPIRE_HOURS = 24;

const SYS_SENDER_ID = 'sys1';

module.exports = {
  EVENT_ID,
  MAIL_EXPIRE_HOURS,
  SYS_SENDER_ID,
};
