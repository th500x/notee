# 战斗纪念图 OSS（必填，否则 POST /api/memorial/battle 返回 502）
OSS_REGION=oss-cn-heyuan
OSS_BUCKET=05-san-storm
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
# 可选：自定义域名或 CDN 根 URL（无则使用 https://{bucket}.{region}.aliyuncs.com/{key}）
# OSS_PUBLIC_BASE_URL=https://cdn.example.com

# 可选：道路 `moveAlongRoad` 诊断（每请求一行 JSON 到 stdout；排查完请关闭）
# SAN_STORM_ROAD_MOVE_DEBUG=1
