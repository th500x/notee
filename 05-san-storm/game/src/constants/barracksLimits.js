/**
 * 编组 / 驻地「军营」中可同时持有的部队卡张数上限（不含主城驻军所仓库内）。
 * 与新手引导文案 `game/src/data/texts/tutorial.js`（普通 20 张等）及卡池文档 11-4 稀有度持有上限口径区分：
 * 此处仅约束「军营栏可见的部队卡」总量，驻军所仓库内卡片不计入。
 */
export const MAX_LINEUP_BARRACKS_TROOP_CARDS = 20;
