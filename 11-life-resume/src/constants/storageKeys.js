/**
 * 11-life-resume frontend constants.
 */

export const STORAGE_KEYS = {
  USER: 'lifeResumeUser',
  TOKEN: 'lifeResumeToken',
  TOKEN_EXPIRY: 'lifeResumeTokenExpiry',
};

/** 与 05 PLAYER_TOKEN_TTL_SECONDS 默认一致 */
export const TOKEN_DURATION_MS = 30 * 24 * 60 * 60 * 1000;
