# Copy to backend/.env on each environment.
# Local XAMPP: DB_USER=root, DB_PASSWORD empty, npm run db:migrate（无 MIGRATION_ASSUME_DB_EXISTS）
# 首次把 05 账号拷进本库：npm run accounts:copy-from-san-storm（不改 05 原表）
# Production 宝塔: 见下方；MIGRATION_ASSUME_DB_EXISTS=1 && npm run db:migrate
#   再以能读 05_san_storm 的账号跑一次 accounts:copy-from-san-storm

PORT=3011
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=11_life_resume

# 11 掌管账号；生产必填。首次上线可沿用旧值，已登录用户不必重登
JWT_SECRET=

# Local ONLY: skip JWT startup check (do not use in production)
# JWT_DEV_BYPASS=1

# --- Production (宝塔 · 与 05/06 同机) ---
# DB_NAME=11_life_resume
# DB_USER=notee.vip.11
# DB_PASSWORD=<宝塔生成的密码>
# MIGRATION_ASSUME_DB_EXISTS=1
# NODE_ENV=production
# JWT_SECRET=<11 自持；首次可沿用旧值>

# Aliyun OSS (P6+; server starts without keys)
# Bucket 名称即 11-life-resume；对象键由程序自动生成
# 浏览器直传须在 OSS 控制台配置 CORS，或运行：node scripts/configure-oss-cors.js
OSS_REGION=oss-cn-heyuan
OSS_BUCKET=11-life-resume
# OSS_CORS_ORIGINS=https://notee.vip,http://localhost:5177
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=

# --- 人生轨迹 lifePath（P12 · 阿里通义 DashScope）---
# 控制台申请：https://dashscope.console.aliyun.com/
# 勿提交真实 Key；本地复制到 backend/.env
# DASHSCOPE_API_KEY=
# DASHSCOPE_MODEL=qwen-turbo
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LIFE_PATH_COOLDOWN_HOURS=24
# LIFE_PATH_MAX_ENTRIES=80
# LIFE_PATH_BODY_MAX_CHARS=400
# LIFE_PATH_AI_TIMEOUT_MS=90000

# --- ETH 1h 均线 Web Push（07 订阅；npm run vapid:generate）---
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=https://notee.vip
# 可选：逗号分隔 4 位账号；空 = 凡登录并授权的账号
# ETH_MA_PUSH_ACCOUNT_WHITELIST=
# 阿里云国内机访问币安常被拦：可设 HTTP 代理，或把 REST/WS 指到可达的反代
# BINANCE_HTTPS_PROXY=http://127.0.0.1:7890
# BINANCE_KLINES_URL=https://fapi.binance.com/fapi/v1/klines
# BINANCE_WS_KLINE_URL=wss://fstream.binance.com/ws/ethusdt@kline_1h
# 国内机访问不了交易所时：Cloudflare Worker 投递用（openssl rand -hex 32）
# ETH_MA_INGEST_SECRET=
# 生产 ingest 交叉后由 Cloudflare 海外代发 Web Push，不必在国内直连 FCM
# 可选：钉死行情源（gate,bybit,binance）；空 = 按该顺序试
# ETH_MA_INGEST_KLINE_SOURCE=
