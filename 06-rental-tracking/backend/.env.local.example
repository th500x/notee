# Copy to backend/.env.local (gitignored). Never enable bypass flags in production.
#
# Optional LOCAL-ONLY: skip JWT verification on admin routes (dev convenience).
# Remove or leave commented for normal JWT behavior (main site login + Bearer token).
# RENTAL_TRACKING_DEV_SKIP_JWT=1
