# Copy to .env.development.local in this folder (gitignored). Vite dev only.
#
# Optional LOCAL-ONLY: treat UI as logged-in admin without token (pairs with backend skip only for local hacks).
# Remove or leave commented for normal admin gate (requires main-site JWT in localStorage).
# VITE_RENTAL_TRACKING_DEV_SKIP_ADMIN=1
