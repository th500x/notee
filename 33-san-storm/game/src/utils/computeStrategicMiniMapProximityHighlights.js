/**
 * 战略缩略图「最近 3 敌对 / 最近 3 中立」高亮 — 实现见 `shared/utils/computeStrategicMiniMapProximityHighlights.js`。
 */
export { computeStrategicMiniMapProximityHighlights } from '@shared/utils/computeStrategicMiniMapProximityHighlights.js';
