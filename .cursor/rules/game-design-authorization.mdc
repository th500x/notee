---
description: 玩法/卡池/概率等规则变更须先征得用户明确同意后再改代码
alwaysApply: true
---

# 玩法与卡池规则 — 变更须先确认

在未收到**明确书面同意**（对话里你回复「可以改」或给出同等效力的确认）之前，**不得**通过改代码调整下列内容（含「为修 bug 而扩大/缩小适用全体玩家的池子或概率」）：

- 卡池范围、势力/ID 段筛选、将领与部队池的包含关系
- 抽取概率、保底、每日上限、补偿（银两/粮草等）规则
- 预览与后端抽卡逻辑是否一致时的**规则取舍**（应优先说明差异，由你决定改配置还是改规则）

**允许且鼓励**：只加诊断日志、只修明显代码错误（不改变上述语义）、**仅改数据/配置**（JSON、数据库种子等）——但若配置变更等价于「新规则」，仍应先说明影响并等你确认。

**流程**：先简述方案、影响范围（例如「会影响所有势力」）、替代方案（例如「仅补某势力配置」）→ 等你同意 → 再提交代码改动。
