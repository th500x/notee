---
description: 禁止未同意的静默 fallback；根因排查（禁止默认值糊数据）；复用已验证逻辑勿擅自重写
alwaysApply: true
---

# 代码质量、兜底与排查

## 1. 禁止未获同意的静默 fallback

在未收到**明确同意**（对话里你表示可以、或指定「这里允许 fallback」）之前：

- **不得**增加「先 A 失败再悄悄用 B」的**静默回退**（双路径 DOM、双数据源、吞错后默认值、注释里「优先…否则回退到…」等掩盖配置/挂载问题的写法）。
- 若你认为某种 fallback 能明显改善健壮性：**先说明两处行为差异与风险并询问**；同意后再写。

**仍允许**：同一抽象下的显式分支（且不跨壳乱查 DOM）；**用户可见**错误提示与 `DEV` 断言；`??` 仅用于「缺字段即非法」且配合校验/早失败。

**动机**：静默兜底会挡住开发期本应暴露的 bug。

## 2. 排查：根因优先，禁止「糊墙」

**生产 / 协作**：不要默认「对方没同步」并把 `git pull` 当首选结论；卡住时对路由、service、SQL、校验、请求路径、是否改错进程/**错误的 `server.js`** 做**逐文件核对**。常见坑：MySQL 结果解构、JSON 列当字符串未解析、API 路径不一致、校验与载荷不一致。

**禁止用临时方案掩盖数据问题**（除非用户明确要求并接受语义变更）：对缺字段用默认值、`||` 兜底；从其它字段猜测；硬编码映射代替真实数据；跳过数据源链路中的某一环不查。

**配置 / 展示类**：沿 `CSV → JSON → 数据库 → API → 前端 → UI / 静态资源` 逐级查，修上游断点而非只在 UI 兜底；修复后做最小回归。

## 3. 复用已验证逻辑

已有**已验证**实现（CSV 脚本算法、线上 service、用户确认「以这份为准」）时：**复用或抽共享函数**，保持行为一致。

**禁止**在未获用户同意时，为「更好看/更简洁」**重写**同一规则并留第二套实现。

**05-san-storm 战术/战役界面**：同一功能（如部队 tooltip、属性行）在 **player / enemy / ally** 之间须共用同一实现与同一数据入口，不得以阵营为借口维护平行代码；见 `san-storm-tactical-ui-consistency.mdc`。

列表类、全量配置读取：**05-san-storm 游戏**侧用 `loadSharedData`（见 `san-storm-data-layer.mdc`），避免各组件复制 `fetch`。
