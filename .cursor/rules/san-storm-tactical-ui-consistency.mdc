---
description: 05-san-storm 战术图/战役图：多阵营（player / enemy / ally）共用同一套数据与 UI 管道，禁止按阵营分叉两套实现
alwaysApply: true
---

# 05-san-storm：战术 UI 与多阵营一致性

## 原则

**玩家（player）、友军 NPC（ally1 / ally2）、敌军（enemy）在「同一块战术/战役地图」上出现时，属同一类战斗实体**：属性展示、tooltip、立绘解析、配置字段来源应走**同一套抽象**，不得因阵营不同再写一套「看起来类似」的分支而不复用。

与 **`san-storm-data-layer.mdc`**（`formatTroopData`、camelCase、单一路径读库）及 **`notee-code-quality-and-debugging.mdc`** §3「复用已验证逻辑、禁止第二套实现」一致；本规则**专门强调阵营维度不得成为随意分叉的借口**。

## 必须统一的部分（示例）

- **部队 tooltip**：小型图与大型图共用 `buildTroopTooltipContent` + `TileTooltipContent`；数值口径（射程、攻防等）与战斗对象上的字段一致，**禁止**仅敌军走另一套拼接或另一数据源。
- **战斗部队对象**：由同一 `formatTroopData` / 同一 API 或同一组装函数填充的字段（如 `range`、`troopType`），各阵营结构一致；仅允许在 **`faction`、`displayName`、立绘 URL 策略** 等「阵营/展示差异」上分支。
- **读库 / 读配置**：`config_troops` 等列表查询与编队、全量配置接口**列清单与别名策略对齐**（例如保留字列统一别名），避免「编队有、全量接口无」的隐性不一致。

## 何时允许差异

仅在有**明确产品设计**（例如敌军不可查看某项信息）时，在**同一组件内**用显式条件渲染；须在注释或规则中说明理由，**不得**复制整段平行实现。

## 修改前自检

新增或改动战术格 hover、战前面板、战役格部队信息时：对照是否三类阵营仍走**同一入口函数/同一 hook 数据**；若发现仅某一阵营单独 `fetch` 或单独 `format`，优先合并为共用路径。
