---
description: San Storm 设计文档须使用既有路径；禁止擅自新建设计类 Markdown，需新建时必须先问用户
alwaysApply: true
---

# San Storm 设计文档 — 路径与授权

## 既定文档位置

- 核心/数据层等设计文档位于 **`05-san-storm/docs/`** 下既有目录（例如 **`05-san-storm/docs/10-core-system/13-1-CITY_SYSTEM.md`**）。
- 仓库根 `.gitignore` 可能忽略 `*/docs/`，**工具检索不到不代表文件不存在**；涉及设计稿时**优先按用户给出的完整路径**读写，不要凭「搜不到」就新建一份。

## 严禁擅自新建设计文档

- **不得**在未获用户**明确同意**的情况下，**新建**设计类 Markdown（含在 `docs/` 下新建文件、为同一主题另起平行文档、或「补写」成独立新文件替代既有编号文档）。
- 若评估下来**确实需要**新建文件、拆分章节、或新增文档编号：**必须先向用户说明理由并询问**，由用户确认路径与文件名后再执行。

## 允许的行为

- 用户**已指定文件**（或任务明确指向某 `*.md`）时：可在该文件内**编辑、增补、修订**，范围以用户任务为准。
- 用户**明确要求**「新建某文档」或「可以新建」后：再按用户给定的路径与命名创建。
