---
description: San Storm 设计文档：既定路径；禁止未读原文覆盖；禁止擅自新建；工具搜不到 docs 仍以用户路径为准
alwaysApply: true
---

# San Storm 设计文档 — 路径与授权

## 既定文档位置（务必以此为准，勿仅依赖搜索）

- **工作区相对路径（推荐给助手）**：`05-san-storm/docs/`  
  例：`05-san-storm/docs/10-core-system/17-1-COMBAT_SYSTEM.md`
- **本机绝对路径（与用户环境一致时直接 `Read`）**：`C:\Google Drive\KIRO\notee\05-san-storm\docs`
- 仓库根 `.gitignore` 常忽略 `*/docs/`，**Glob / Grep / 语义搜索可能返回空**；**这不表示目录不存在**。查找《三国风云》/ San Storm 设计说明时：**直接对上述路径做 `Read` / `Glob`（带完整前缀 `05-san-storm/docs`）**；若仍失败再以用户当次提供的路径为准，**禁止**因「搜不到」就臆测或新建文档。

## 编辑既有设计文档（**强制：先读后改**）

- **绝对禁止**在未对**该路径**执行 **`Read`（完整读取）** 的前提下，用 **`Write` 整文件覆盖**、或另起一篇顶替**同主题编号文档**（例如已有 `13-3-*.md` 却新建平行文件替代）。
- **正确流程**：用户给出路径（或任务指向某 `*.md`）→ **`Read` 全文或关键章节** → 使用 **`StrReplace` / 分段编辑** 在**原文基础上**增补修订；仅在确认文件不存在且用户同意新建时，才 **`Write` 新建**。
- **Glob / 语义搜索返回空**：**不得**作为「没有原文」的依据；**必须以用户提供的绝对/工作区路径 `Read` 结果为准**。若 `Read` 失败，向用户说明并请求确认路径，**禁止**默认新建。

## 严禁擅自新建设计文档

- **不得**在未获用户**明确同意**的情况下，**新建**设计类 Markdown（含在 `docs/` 下新建文件、为同一主题另起平行文档、或「补写」成独立新文件替代既有编号文档）。
- 若评估下来**确实需要**新建文件、拆分章节、或新增文档编号：**必须先向用户说明理由并询问**，由用户确认路径与文件名后再执行。

## 允许的行为

- 用户**已指定文件**（或任务明确指向某 `*.md`）时：**先 `Read` 再改**，在该文件内**编辑、增补、修订**，范围以用户任务为准。
- 用户**明确要求**「新建某文档」或「可以新建」后：再按用户给定的路径与命名创建。

## 与 Git 的关系

- `05-san-storm/docs/` 下设计文档**默认不入库**（见 `notee-monorepo-layout-and-backends.mdc`）。助手**不得**为「让协作者看到文档」而 **`git add -f` docs**；共享约束请放在**允许入库**的路径（如根目录 `README.md` / `AGENTS.md`），由维护者自行决定。
