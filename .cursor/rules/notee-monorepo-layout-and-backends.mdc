---
description: notee 多项目：目录变更先问用户；文件与后端须在对应子项目；端口/PM2；docs 与 Git 关系
alwaysApply: true
---

# Monorepo 布局、后端与文档同步

## 目录 / 项目边界

**须事先征得用户同意**：新建或移动顶层与子项目目录、重组/合并/拆分项目。

**布局约定**：

- **主页**：仓库根 `src/`、`index.html`、`package.json`、`vite.config.js` 等。
- **子项目**：`XX-name/` 内自有 `src/`、`index.html`、`package.json`、`vite.config.js`。
- **全站共享后端**：根目录 `backend/`（仅共享能力）。

**文件落位**：

- 业务代码、路由、配置只能在**所属子项目**内（例如 `06-rental-tracking/`，不要写到根 `backend/`「顺便加路由」）。
- 改前确认 **PM2 / 启动脚本实际 `cwd` 与 `server.js` 路径**，避免改到未被加载的文件。

## 子项目独立后端与端口

- 各子项目自有 `backend/`（或约定目录），**不要**把 A 业务写进 B 的 `server.js`。
- 根 `backend/` 只承载留言板、认证等**全站共享**能力。

| 服务 | 目录 | 端口 | 职责 |
|------|------|------|------|
| 主站共享后端 | `backend/` | **3001** | 留言板、认证等 |
| 新闻日历 | `01-news-calendar/backend/` | **3002** | 01 专属 |
| 租赁追踪 | `06-rental-tracking/backend/` | **3003** | 06 专属 |
| 真三风云 | `05-san-storm/backend/` | **3005** | 05 专属 |

以各项目环境变量与启动说明为准：**一进程一端口，不串项目**。

## PM2 / 多进程

- 根目录 `ecosystem.config.cjs`（若存在）：只配**共享**后端（cwd 指向仓库根、`PORT: 3001`）。
- 子项目 `ecosystem.config.cjs`：只配**本项目**后端（cwd 指向该子项目、对应端口）。避免 cwd 与业务错配。

## `docs/` 与 GitHub

根 `.gitignore` 含 `docs/`、`*/docs/`：**各项目 `docs/` 默认不提交**。协作者可能看不到你的 `docs/` 副本。

**需随仓库共享的约束**：放在 `.cursor/rules/`。若要改为部分 `docs` 入库，须**显式改 .gitignore 并征得维护者同意**。
